# Leadstack CRM — v20-AI

Adds AI endpoints (protected by `x-api-key`), logs AI outputs to Activities, and keeps your v19 chat + click-to-call base.

## Run
- Import to Replit
- Add env from `.env.example`
- Start → open `/health` → `/`